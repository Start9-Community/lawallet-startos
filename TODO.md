# TODO

- [ ] Replace `icon.svg` (currently the LaWallet isotype) with a square app
      icon if upstream produces one.
- [ ] Upstream describes LaWallet NWC as pre-alpha. Re-check the environment
      surface (`apps/web/lib/config/env.ts`, `apps/listener/src/env.ts`) on each
      bump until it stabilizes.
